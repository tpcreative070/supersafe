<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:clipChildren="false"
    android:clipToPadding="false"
    android:gravity="center"
    android:orientation="vertical">

    <LinearLayout
        android:id="@+id/button"
        android:textStyle="bold"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/rectanglebutton_selector"
        android:clickable="true"
        android:gravity="center">

        <ImageView
            android:padding="6dp"
            android:layout_gravity="center"
            android:id="@+id/buttonImage"
            android:layout_width="@dimen/default_verify_button_size_height"
            android:layout_height="@dimen/default_verify_button_size_height"
            android:adjustViewBounds="true"
            android:clickable="false"
            android:focusable="false"
            android:scaleType="fitCenter"
            android:src="@drawable/baseline_check_white_48"/>
    </LinearLayout>
</LinearLayout>