// Top-level build file where you can add configuration options common to all sub-projects/modules.

buildscript {
    
    repositories {
        google()
        jcenter()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:3.2.1'
        // NOTE: Do not place your application dependencies here; they belong
        // in the individual module build.gradle files
    }
}

allprojects {
    repositories {
        google()
        jcenter()
        flatDir {
            dirs 'libs'
        }
        maven { url 'https://jitpack.io' }
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}


subprojects {
    ext.appVersionCode = 172
    ext.appVersionName = "1.7.2"
    ext.appMinSdkVersion = 19
    ext.appSupportVersion = "28.0.0"
    ext.appCompileSdkVersion = 28
    ext.appBuildToolsVersion = "28.0.3"
    ext.appTargetSdkVersion = 28
    ext.appConstraitLayout = "1.1.0"
    ext.build_versions_java_version = 1.8
    ext.appRoomVersion = "1.1.1"
    ext.fresco = "1.10.0"
    ext.glide = "4.8.0"
}
